#include <stdio.h>
void main()
{
	unsigned char a, b, c;
	printf("VVedite a: ");
	scanf_s("%i", &a);
	printf("VVedite b: ");
	scanf_s("%i", &b);
	c = a / b;
	printf("Otvet: %i", c);
	system("  pause");
}
